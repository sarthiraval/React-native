hook can only feature of class based components in function based components.
hook can not inside any function, if statement and loops.
hooks can only for be called at the top level of a components.

rules of hooks:-
      hooks can only be called inside React function components.
      hooks cannot be conditional
      hooks can only for be called at the top level of a components.


 type of hooks:-
    useState :- const [count,setCount] = useState()
            
             useState is a react hook that lets you add a state variable to your component.
             count  = present
             setCount = update

      useState in many update  without declare in function to override function.
      setCount()
      useState in many update  with declare in function to .

    useEffect :- 
    useRef:-
    useContext:-
    useMemo:-